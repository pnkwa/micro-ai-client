# Dev proxy target — the actual backend URL the Vite proxy forwards /api/* to.
# The client always talks to /api (relative), the proxy strips that prefix.
NUXT_PUBLIC_API_BASE_URL_TARGET=http://localhost:3000

# Set to true to bypass all auth checks (demo / dev without a live backend)
NUXT_PUBLIC_AUTH_DISABLED=false
